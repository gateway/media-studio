#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Release verification is intentionally deterministic and repo-tracked only.
# Local browser/live smoke runs now live under ignored developer paths.
"$SCRIPT_DIR/run-quality-gates"
