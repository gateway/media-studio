# Media Presets

Store portable Media Preset bundles here.

Expected export format:

- `.media-studio-preset.zip`
- contains `manifest.json`
- may contain thumbnail assets under `assets/`

Keep generated output examples out of this folder unless they are tiny documentation screenshots with an explicit reason to be committed.
