# Engineering Work State

Last updated: 2026-08-15
Current objective: close the superseded Media Assistant orchestration backlog against the current kernel/runtime.
Active task ID: None
Current phase: Media Assistant orchestration closeout
Status: COMPLETE. The confirmed environment-branch failure is covered by a no-network typed-provenance regression, the stale `Thinking…` concern has a focused delayed-success UI regression, vague human-language browser guidance passes without side effects, and all historical MAO tasks are completed or crosswalked to successor campaigns.

## 2026-08-15 — Media Assistant orchestration backlog closed

- Prompt Recipe outputs now carry typed execution semantics for environment, storyboard metadata, and supported reference-analysis recipes. Storyboard art-only outputs reuse their existing typed source contract.
- GPT Image 2 storyboard preflight follows typed provenance. Unknown and legacy workflows retain the conservative content-based fallback.
- The exact MAO environment failure was reproduced before the fix and passes afterward without a network request, media job, or credit spend.
- A delayed successful Assistant response clears the visible busy state without refresh. A fresh two-turn browser conversation accepted vague sci-fi ideation, offered concise creative guidance, asked one useful question at a time, and did not mutate or run the graph.
- The companion MAO task list is the authoritative retirement crosswalk. No MAO task remains active.

## 2026-07-23 - Media Assistant reference-storyboard Phase 5 complete

- Canonical fix contract: [spec](MEDIA_ASSISTANT_STORYBOARD_FIX_SPEC.md) and [tasks](MEDIA_ASSISTANT_STORYBOARD_FIX_TASKS.md). The live failure exposed four cross-layer seams not covered by the original synthetic test: `graph` matching inside `photographic`, reference-prompt/preset instruction collision, premature graph auto-apply, and shot/style parsing of incidental or negated terms.
- Shipped behavior: attached/reference-image storyboard requests with no explicit count produce eight chat shots; the story route excludes Media Preset orchestration; only explicit graph approval suggests/applies a plan; ordered shot headers ignore backward continuity references; negated fantasy/science-fiction phrases do not become positive style tags.
- Browser proof: disposable session `asst_889e59f57505`, reference `138c374c-d8be-49bf-aa28-c2d1bf06e21c.png`, eight stored/compiled shots, 23 local operations, 11 applied nodes, persisted canvas and applied assistant state after reload, immediate undo available, `run_requested=false`. Credits 517.62 and latest job/run IDs `job_ab6c459f93e2` / `grun_44a5864f47ba` remained unchanged; no Run or workflow Save occurred.
- Automated closure: 17 focused incident regressions, 375 complete Media Assistant tests, 3 prompt-asset tests, 86 focused web tests, typecheck, lint, theme drift, and diff hygiene pass. Full release verification remains blocked only by the unrelated pre-existing 7,001/7,000-line `test_graph_studio.py` boundary.

## Resume After Compaction

There is no active task. Before opening new work, read this file and the relevant current task/spec documents; do not resume a historical phase from the evidence below.

## Historical Phase 33 Evidence

- Phase 33 is terminally complete. Zero-credit preflight `grun_02e170898650` compiled three six-panel sheets at 3,945 / 3,829 / 3,913 characters with exact dialogue, no contraction corruption, and no media jobs. Character is `@image1`, Environment is `@image2`, and Boards 2-3 use the immediate previous board as `@image3`.
- Paid run `grun_95ca568765eb` completed Board 1 `job_825880176d32` / raw `asset_22f4fbaad9bf`, Board 2 `job_68733fa6c331` / raw `asset_b67cf3c0c12f`, and Board 3 `job_bbd359f4ebc0` / raw `asset_cfe6dfc13a98`. Saved 2048x1152 sheets are `asset_graph_8160a296ed46fdf43f38aa9a`, `asset_graph_3bf3103063e8610509a599f0`, and `asset_graph_b3b1198cc4b373d8dd9c283f` in Sadi (`project_ab78ce28660d`).
- Normalized requests prove Board 1 = Character + Environment, Board 2 = Character + Environment + this run's Board 1, and Board 3 = Character + Environment + this run's Board 2. Media jobs moved 720 -> 723, graph runs 789 -> 792 after two zero-cost preflights plus the paid run, media assets moved 679 -> 685, and provider balance moved 189.62 -> 159.62: exactly 30 credits / $0.15.
- Original-resolution review passes the revised story: exactly two failed units are revealed, two clean replacements are presented, both failed units are removed leaving two sockets, two clean units are installed with green confirmation, Bolts walks beside the pilot up the ramp, climbs into the adjacent chair, speaks while secured, and the same single ship lifts with a clear floor gap and exits the north doors. All requested dialogue appears on the saved sheets.
- The saved workflow keeps the revised Board 2-3 story in the active `continuation_brief` fields; older `user_prompt` values are inactive legacy fields. Additional source locks explicitly preserve the two-for-two repair and Bolts' walk-to-chair action. All 12 recipe/art/compositor/Save nodes are Frozen, the estimate is approximately 0 credits / $0, balance is 159.62, API/runner are healthy, queue is 0/0, and database integrity is `ok`.
- Pre-change backup: `data/backups/media-studio-before-phase33-graphwf_4fd06f50c493-20260719T034315Z.db`, integrity `ok`, SHA-256 `07f9b4b461c5cc46ac814f4f66a406918c84e8d085ceacc3f171c52a1ddc1384`.

- PAID-3203 / VERIFY-3204 are terminally complete. Browser preflight refreshed pricing to `2026-07-19-site-pricing-page`, proved a healthy empty queue, database integrity `ok`, exact 10-credit / $0.05 estimate, only Board 3 GPT/compositor/Save enabled, and Boards 1-2 Frozen before the one Run click.
- Run `grun_3e468f355396` completed Board 3 `job_978911d388e6` / raw `asset_06af0960023d`, composed reference `ref_a101ca1c93b1`, and saved 2048x1152 sheet `asset_graph_cc5b59dd67dc246f10a1fa64` in Sadi (`project_ab78ce28660d`). Media jobs moved 719 -> 720, graph runs 788 -> 789, media assets 677 -> 679, and balance moved 199.6 -> 189.6: exactly 10 credits / $0.05.
- The actual normalized request proves Board 3 used Character first, Environment second, and accepted PAID-3201 Board 2 raw art `asset_cc82aca40ca8` third. Its three local input paths match those exact retained sources; Boards 1-2 ran only from Frozen caches belonging to `grun_e2afaa39b574`.
- Original-resolution review passes identical 2048x1152 layout v4 chrome/fonts/geometry, complete readable five-row metadata, recurring covered pilot identity, weathered red-and-cream ship/hangar Environment, Board 2 Panel 6 -> Board 3 Panel 1 adjacent-but-advanced movement, ramp boarding, cockpit entry, a compact feline Bolts with mechanical forelegs, two secured occupants, and final north-door departure.
- Strict trilogy signoff is withheld. Board 3 Panel 5 shows the occupied cockpit looking at a second matching ship hovering outside the canopy. This breaks single-ship continuity and does not unambiguously prove vertical lift of the current ship. The successful outputs remain evidence; no retry is authorized or implied.
- Closure passes: balance 189.6, API/runner healthy, queue 0/0, database integrity `ok`, all nine storyboard art/compositor/Save nodes Frozen, and browser estimate approximately 0 credits / $0. No recipe/layout/schema/topology/provider/model/pricing or Character/Environment change occurred.

- PAID-3201 / VERIFY-3202 are terminally complete. Browser preflight proved a healthy empty queue, database integrity `ok`, three enabled Save branches, exact 30-credit / $0.15 estimate, and the required ordered reference chain before the single Run click.
- Run `grun_e2afaa39b574` completed Board 1 `job_e795f18b3f4f` / `asset_677de9a66248` and Board 2 `job_a86146618b44` / `asset_cc82aca40ca8`. Saved sheets are `asset_graph_1e903703e74422d26657f56d` and `asset_graph_e9e02eb7d91cf74f0f31ea3d`, both 2048x1152 in Sadi (`project_ab78ce28660d`).
- Board 3 `job_9641977d3661` failed with a transient provider internal error before compositor/Save execution. Balance moved 219.6 -> 199.6, exactly 20 credits / $0.10. Media jobs moved 716 -> 719 because the failed provider request is retained as evidence.
- The actual normalized requests prove ordered refs, not merely graph badges: Board 1 used Character then Environment; Board 2 used Character, Environment, then this run's Board 1 raw art; Board 3 was submitted with Character, Environment, then this run's Board 2 raw art before the provider failed.
- Original-resolution inspection passes identical Board 1-2 layout/fonts/chrome/metadata geometry, complete readable rows, recurring covered character identity, red-and-cream ship/hangar Environment, cinematic treatment, repair progression, and the adjacent-but-advanced closed-amber-panel -> latch-release handoff. Full 18-frame/Bolts/Board 2 -> 3 visual signoff is unavailable because Board 3 has no new image.
- Focused post-run verification passes 110/110 storyboard metadata/spec/compositor/trilogy tests, the recipe/compiler genericity guard, database integrity, and `git diff --check`. API/runner are healthy, queue is empty, all nine storyboard art/compositor/Save nodes are Frozen, and the browser estimate is approximately 0 credits / $0.
- PAID-3203 authorization is consumed. Do not regenerate or retry any board without a new explicit authorization.

- BUG-3110 is closed. Full-run hydration now refreshes a completed node's reusable cache only from artifacts matching that exact run, node, and output port. Failed, empty, partial, and mismatched results retain the previous known-good cache. The user flow that failed before—hydrate an overall failed run, then freeze a previously completed node—now serializes the newest `cached_run_id` and artifact IDs.
- Verification passes 32 focused hydration/runtime/serialization/lifecycle tests, all 762 web tests, all 900 backend tests, lint, typecheck, production build, recipe genericity, file-size/style/hygiene checks, Studio smoke, and `git diff --check`. In-app Graph Studio reloads with no console errors; its visible estimate remains approximately 0 credits / $0 but is marked stale after reload, and the Frozen/Cached nodes remain intact. No provider job ran, no credit was spent, and the signed-off assets/workflow content were not changed.
- Phase 31 is governed by SPEC-036. Enlarged actual-output inspection and the antialiased-pixel feedback loop disproved the prior opaque-pixel clearance result: 18 wrapped Phase 30 rows had only 1px lower clearance.
- The renderer now requires 3px antialiased lower-rule clearance, preserving layout v4 and stepping down only affected wrapped values from 15px to the existing 14px floor. The exact offline trilogy has 15 wrapped rows, 3px minimum clearance, and zero semantic fragments.
- The typed spec removes an equivalent repeated CAMERA-contract clause without dropping framing. Board 2's duplicated high-angle/locked-off/50mm tail is removed in exact replay.
- Browser preflight passed at 249.6 credits and exactly 30 credits / $0.15. Three successful provider jobs produced Board 1 `asset_54d60149cdc0`, Board 2 `asset_5920b3b27555`, and Board 3 `asset_d8c710d43e26`; the one rejected Board 3 attempt consumed 0 credits. Total balance is 219.6, exactly 30 credits spent.
- Saved sheets are Board 1 `asset_graph_110ad4f2d56a07cebafd72ea`, Board 2 `asset_graph_ed294a0aa592ffc959edb495`, and Board 3 `asset_graph_329d7b42dcf91a78638ac792`. All six assets belong to Sadi (`project_ab78ce28660d`).
- Original-resolution inspection passes identical layout/fonts/chrome, cinematic action, character/environment continuity, both adjacent-but-advanced handoffs, repair/boarding/launch progression, and feline cyborg Bolts. Across 15 wrapped rows the minimum glyph-to-rule clearance is 3px with zero failures.
- Provider-facing visual context now converts negative identity/wardrobe exclusions to affirmative directions. Exact Board 3 preflight was 3,982 characters and excluded `never`, `underwear`, `expose`, `humanoid`, `bipedal`, and `midriff`.
- All paid nodes are refrozen and Graph Studio estimates approximately 0 credits / $0. API/runner are healthy and the queue is empty.
- Final post-run verification passes 900/900 backend tests in 264.06s, compilation, recipe/compiler genericity, database integrity, and `git diff --check`.

- Phase 30 is governed by SPEC-035. The renderer now centers actual glyph bounds; focused storyboard tests pass 129/129 and the full backend passes 896/896. `tmp/phase30-proof/board-1-glyph-safe.png` passes original-resolution inspection with pixel-identical cinematic rectangles. Genericity, compilation, and diff checks pass.
- Pre-paid backup `data/backups/media-studio-before-phase30-paid-20260717T053627Z.db` reports integrity `ok`; SHA-256 `6c9399d7ad8baf48427f92eb72b957ba90a59040712b4c604b28ab7a859bfcec`. Baseline media jobs are 709 with 0 queued / 0 running.
- PAID-3005 was consumed exactly once by `grun_c14f1f0b747a`. Three provider jobs completed for 30 credits / $0.15; balance is 249.6. All three storyboard providers are refrozen, the graph estimates approximately 0 credits / $0, and the queue is empty.
- Browser preflight passed at 279.6 credits and ≈30 credits / $0.15. Run `grun_6c19e98fb442` stopped before provider submission because the finished-sheet metadata preflight incorrectly inspected the typed text-free art prompt. No media job or credit was created; all three storyboard models were immediately refrozen and the estimate returned to ≈0 / $0.
- BUG-3007 now recognizes only typed, explicitly text-free storyboard art-source prompts as outside finished-sheet row validation. The exact failed prompt returns no sheet preflight; visible sheet prompts retain strict row validation. Focused tests pass 130/130 and the clean complete backend rerun passes 897/897. An unrelated Media Assistant order-sensitive test failed once in an intermediate run, passed alone, and passed in the clean complete rerun.
- Original-resolution paid inspection proves identical layout v4 geometry on all three composed sheets and 4-13 px lower clearance for every non-empty metadata value. The two-line clipping defect is closed.
- The paid trilogy passes environment, character/covered wardrobe, cinematic quality, causal flow, both inter-board handoffs, dialogue, and launch. It is not a global visual signoff: Board 2 Panel 2 repeats its CAMERA description, and Board 3's Bolts is feline-faced but upright/human-shaped rather than compact four-legged feline anatomy.
- BUG-3009 updated the formal quality audit for the typed art-only/compositor architecture. Its focused tests pass 7/7, the affected storyboard target passes 101/101, and the post-change complete backend passes 898/898. The paid manifest passes every deterministic gate and records V001/V008 as the two visual exceptions.
- In-app Browser exposes Board 1 and Board 2 composed previews. Board 3's completed 2048x1152 reference `ref_16628910c88b` exists and passes direct original-resolution inspection, but its Graph node still says `No preview yet`; BUG-3012 records that separate UI hydration defect.

- Phase 29 is governed by completed SPEC-034. AUDIT-2901 through BROWSER-2907 are closed. Post-review artifacts are under `docs/reviews/20260716_220119/`; no medium-or-higher issue remains.
- No provider action is authorized. PAID-2908 is optional and requires separate action-time authorization for new art-only Board 1-3 sources before a full three-board composed-image proof.

- Phase 28 is governed by SPEC-033. `asset_338f93c9c12b` remains read-only visual/prompt evidence. TEST-2802 through BROWSER-2806 are complete; the selected Character, Environment, user story inputs, reference order, 23-node/37-edge topology, and all provider/save Frozen states are preserved.
- No provider action is authorized. PAID-2807 is optional and requires fresh action-time authorization for one same-contract trilogy followed by VERIFY-2808 original-resolution inspection.

- Phase 27 is governed by SPEC-032. Preserve the selected Character, Environment, story inputs, reference order, 23-node/37-edge topology, provider/model/pricing settings, and all Phase 26 assets. The approved presentation now uses a 4:3 row-major 2x3 source plate, deterministic final 3x2 reflow, image-dominant panels, and compact exact metadata.
- PAID-2704 is consumed. `grun_b8c24c36819a` spent 10 credits on accepted Board 1; Board 2 was canceled/refunded and Board 3 was not submitted. All six Storyboard GPT/Save nodes are Frozen. PAID-2706 requires fresh authorization for Boards 2-3 only at an estimated 20 credits / $0.10.

- Phase 26 is closed. Keep `grun_26f886636aea` preserved and the six Storyboard GPT/Save nodes Frozen/Cached at an estimated 0 credits / $0. A future paid proof must be separately authorized. No current paid action is allowed.

- Phase 25 is complete. The next safe action is optional only: separately authorize one paid art-only trilogy if a new real-image proof of the compositor path is desired. Do not run or unfreeze provider nodes without that authorization.

- TEST-1001 through VERIFY-1005 and PLAN-1101 through VERIFY-1103 are complete. Phase 12 is newly authorized under [SPEC-024](SPEC.md#spec-024---phase-12-is-a-full-trilogy-cinematic-quality-gate).
- Phase 12 work is complete. [PAID-1204](TASKS.md#paid-1204---execute-up-to-six-complete-trilogy-attempts) consumed the authorized six complete attempts and [VERIFY-1205](TASKS.md#verify-1205---freeze-and-sign-off-the-best-complete-trilogy) froze attempt 5 as the best same-run trilogy.

- STORY-701, GRAPH-702/708, BUG-706/707/709/712/714, VERIFY-703, PAID-704/713, and VERIFY-705 are complete or terminal.
- [PAID-704](TASKS.md#paid-704---run-one-approved-four-image-proof) was consumed once. Environment succeeded; Board 1 was provider-policy rejected; Boards 2/3 never submitted.
- [BUG-712](TASKS.md#bug-712---replace-the-policy-fragile-character-reference) is complete with a neutral private-text-free identity portrait and the successful Environment frozen as the exact downstream lock.
- PAID-713 was explicitly authorized and attempted once. Board 1 was again provider-policy rejected; Boards 2/3 never submitted; credits remained 959.6.
- BUG-714 is complete: the exact failed paid raw format now compacts to six full panel capsules with positive-only workwear/framing language.
- BUG-715 is complete: selected-node replacement and frozen-cache hydration/serialization pass automated and real-browser proof.
- PAID-716 / VERIFY-717 are complete: one newly authorized Board 1-only proof succeeded and was visually reviewed; no retry or Board 2/3 submission occurred.
- Per the user's explicit direction, the Character selection was not replaced during the run and the saved workflow now keeps the browser-attached `asset_901adc9b21d1` sheet.
- Seedance remains out of scope. The six-attempt Phase 12 ceiling is exhausted; no seventh run is authorized.

## Last Known Progress

- Layout v4 restores the selected historical hierarchy: thin outer amber frame, unified inline production band, 30px board title, 20px SHOT headings, near-black metadata, five 30px rows, and 352px cinematic image height in each 538px panel. SHOT remains typed but appears only once above the image.
- Storyboard v2 `2.24` and Continuation `1.19` share one immutable display/output contract. Generated SHOT/CAMERA/ACTION/MOTION are compacted only at complete generic boundaries; exact user-owned DIALOG and panel NOTES remain authoritative and fail clearly if they cannot fit. The genericity guard passes.
- Final offline render: `tmp/phase29-proof/board-1-layout-v4-final.png`. Focused Phase 29 regressions pass 128/128; the full backend passes 895/895; compilation, genericity, and `git diff --check` pass.
- In-app Browser proof `grun_0c441e491fd6` advanced all three recipes/typed compilers and rendered Board 1 layout v4 at 2048x1152. Board 2 then correctly rejected its incompatible frozen historical complete-sheet cache. Balance remained 279.6, media jobs remained 709, queue returned to 0/0, and graph estimate remained approximately 0 credits / $0.
- The Graph Studio saved workflow was not overwritten. Reloading the previously unsaved Sadis tab exposed a blank local New workflow tab; the saved Sadis Adventures workflow was reopened read-only for verification. Historical assets, Character, Environment, story inputs, graph topology, reference order, provider/model/pricing, and Frozen states remain unchanged.

- Phase 28 layout v3 uses one compact amber/near-black top band, one SHOT heading above each image, and exactly CAMERA/ACTION/MOTION/DIALOG/NOTES below it. Removing the duplicate SHOT row returns the space to the cinematic frame; title, production strip, borders, typography, and row geometry are deterministic across boards.
- Source compatibility is provenance-based and campaign-agnostic. A single provider asset must prove `storyboard_art_grid_v1` plus `2x3`, or its prompt must match the current 4:3/2x3 art-only contract. Six ordered images and operator-owned manual reference grids remain supported. Historical complete sheets fail before slicing.
- Verification passes 111 focused storyboard tests and all 871 backend tests. Exact Board 1-3 replay is 3,411 / 3,661 / 4,094 characters, all below 4,200 and compatible; genericity, compilation, node-catalog refresh, deterministic repeat rendering, byte-identical rerender comparison, and `git diff --check` pass.
- Browser proof `grun_be172e6a307c` cost $0 and created zero media jobs. Board 1 completed at 2048x1152 in layout v3; Board 2 correctly rejected the historical complete-sheet cache and Board 3 skipped upstream. Balance stayed 279.6, graph estimate stayed approximately 0 credits / $0, and queue returned to 0/0 healthy.
- Backup `data/backups/media-studio-before-phase28-source-audit-20260716T140333Z.db` passes integrity check with SHA-256 `22547da236a4a714e18588c78a5807fff0583a5d167d5ac1224db59f416042b6`. Exactly one payload changed: accepted Board 1 art `asset_fffdf76a897c` received generic audited source-contract/grid lineage; the image, prompt, references, and workflow are unchanged.

- Phase 27 replaces destructive wide cropping with a 4:3, row-major 2-column by 3-row source plate and deterministic final 3-column by 2-row reflow. The final sheet keeps the approved dark/amber footer-free design, gives art 66.8% of each panel, caps metadata at 33.2%, and reports `wide_2x3_source_grid` plus its safe-frame contract.
- Paid attempt `grun_b8c24c36819a` produced accepted Board 1 `asset_fffdf76a897c` for 10 credits / $0.05. Board 2 was canceled/refunded after the source-grid mismatch was discovered; Board 3 was not submitted. Current balance is 279.6.
- Zero-credit proof `grun_8266439febee` saved the corrected Board 1 as `asset_graph_c75aa12f04307fd5b67c25bf`. Exact Board 1-3 prompts are 3,355 / 3,605 / 4,038 characters and carry the 4:3 / 2x3 source contract. All 865 backend tests, the storyboard genericity guard, compilation, and `git diff --check` pass. The workflow is refrozen at approximately 0 credits / $0 with queue 0/0.

- Phase 26 completed exactly one paid trilogy as `grun_26f886636aea`: Board 1 `asset_e4e3c74424b1`, Board 2 `asset_f809f9d705ef`, and Board 3 `asset_b148e11a5b73`; saved compositor outputs are `asset_graph_53cca9af05cad858f80a4047`, `asset_graph_22ab6d2eeb3399c510fe6219`, and `asset_graph_8d13bd05371d4b49cef8a6ff`. Balance moved 319.62 -> 289.62, exactly 30 credits / $0.15, with no retry.
- The repeated/masked metadata defect is closed. Both built-ins require distinct row semantics and expose the same user-owned Panel Notes Cues field; schema 51 is current. All 18 ACTION/MOTION/NOTES sets are populated, row-local, distinct, and rendered exactly by `StoryboardSheetSpec` v1.
- Original-resolution inspection confirms identical 2048x1152 layout geometry across all three boards. A wide-view inspection artifact initially appeared to clip Board 2's title/strip; a local re-render produced the exact same SHA-256 and a focused crop confirmed the complete title and metadata.
- Strict visual art review passes cinematic treatment, covered recurring pilot, ship/hangar family, repair progression, differentiated handoffs, exact metadata, and dialogue. It withholds final art signoff because Board 1 Panel 1 is not the requested full-ship-length establishing view, Board 1 Panel 4 visually suggests an open recess, Bolts remains too humanoid rather than a compact quadruped feline cyborg, and Board 3 Panel 5 depicts exterior lift instead of the requested cockpit two-shot.
- Post-paid audit found that the 4,200-character art compiler could join a word-truncated MOTION clause directly to NOTES. `_bounded_art_clause` now terminates at a complete generic clause boundary. Exact replay produces 3,702 / 3,622 / 4,026 characters with six cells and no joined-clause failures; this no-paid fix has not received a second paid proof.
- Full release verification passes: 863 backend tests, 758 web tests, lint, typecheck, production build, file-size/style/hygiene, schema 51, recipe/compiler genericity, and `git diff --check`.
- In-app Browser verified `grun_26f886636aea` in Run History with 23 nodes / 40 artifacts, the three current GPT assets and three saved compositor assets, 289.6 displayed credits, and a refrozen graph estimate of approximately 0 credits / $0. BUG-2610 now hydrates historical KIE spend from provider job provenance and displays this paid run as `30 cr` rather than `$0.00`.

- Phase 25 separates story generation from production-sheet rendering. Both Storyboard v2 recipe families compile into `StoryboardSheetSpec` v1; `storyboard.compile` emits a validated art-only prompt and typed spec, and `image.storyboard_sheet` renders exact 2048x1152 title, production strip, 3x2 geometry, borders, and SHOT/CAMERA/ACTION/MOTION/DIALOG/NOTES rows outside the generative model.
- BUG-2412 is complete. Meaningless SHOT values and the new semantic-fragment families fail closed; exact PAID-2410 replay shapes to 4,168 / 4,189 / 4,193 characters, all eighteen panels pass, Board 3 retains `mechanical forelegs` and `cyan status lights`, and the duplicate-title-region lock remains present.
- The online database backup `data/backups/media-studio-before-phase25-graphwf_4fd06f50c493-20260715T2120.db` passes `integrity_check=ok` with SHA-256 `1d597e52916607da18238039133431c225e2a9db4df2aebc6bd255d1743c7ec6`. Only `graphwf_4fd06f50c493` was migrated; historical caches and the unrelated Time Stop tab were preserved.
- In-app Browser verification shows each compiler/sheet node exactly once, both types in the node palette, Sadis clean, graph ≈0 / $0, 319.6 displayed credits, Run enabled but untouched, and no console warnings/errors. Offline current-artifact composition confirms identical chrome across all three boards; a Unicode TrueType font chain prevents missing-glyph boxes for em dashes and smart punctuation.
- Final verification: 91 focused/regression tests and 844/844 full backend tests pass. Earlier phase-wide gates remain green at 758 web tests, web typecheck, lint, file-size/hygiene, node-catalog refresh, genericity, compilation, duplication ownership, and `git diff --check`.

- PAID-2410 completed as `grun_dcdcf40aa2b2` with Board 1 `job_0393797ea128` / `asset_7305c2b60a93`, Board 2 `job_9318479a152c` / `asset_79a34fbb80fe`, and Board 3 `job_b1be5f7e57f5` / `asset_338f93c9c12b`; all are 2048x1152. Browser balance moved 349.6 -> 319.6, exactly 30 credits / $0.15. Media jobs moved 701 -> 704, one per board, with no retry or duplicate submission.
- Full-resolution Browser review passes V002-V007 and V009-V010: board numbering, cinematic physical-set quality, Environment authority, recurring covered pilot, causal 18-frame flow, both adjacent-but-distinct handoffs, exact attributed dialogue, and final departure. V001 fails because Board 3 adds duplicate pre-image panel-title strips and provider-bound metadata includes incomplete values. V008 fails because Bolts is an ordinary tabby without the requested mechanical forelegs/cyan lights, and Board 3 Panel 4 omits Bolts from the BOTH SECURED beat.
- Root cause is deterministic: preflight/D001 does not require a meaningful SHOT suffix and misses curly possessives, terminal adjective/participial phrases, and short noun-only motions. Board 3's compact prompt retains `cyborg cat` but drops the user-owned mechanical-foreleg and cyan-light traits. Manifest `data/quality-manifests/grun_dcdcf40aa2b2.json` is deterministic `pass`, visual `needs_review`, overall `not_accepted`.
- The workflow was refrozen and saved after review: eight Frozen media/save nodes, approximately 0 credits / $0, balance 319.6, queue 0/0, and no further paid authorization. Because PAID-2410 failed strict visual acceptance, its three outputs remain preserved in run history as evidence while the saved frozen Storyboard cache continues to point to prior accepted run `grun_97a000ce7558`. BUG-2412 is the next no-paid task.
- BUG-2408 adds one generic CAMERA semantic contract at the provider boundary and extends semantic-fragment detection/repair for terminal transitive predicates, preposition/adjective tails, successive-object truncation, and noun-list fragments. Tight-budget fitting now preserves angle/movement/lens CAMERA components and complete ACTION/MOTION/NOTES clauses without adding campaign story content.
- Exact zero-credit Browser proof `grun_814d284c8c6f` completed 17/17 in 232.0 seconds with eight media/save nodes Frozen/Cached. Balance remained 349.6 credits, media jobs remained 701, graph cost remained approximately 0 credits / $0, and the queue returned to 0/0.
- Current-code replay of the new raw Board 1-3 artifacts shapes 11,573 -> 4,110, 12,947 -> 4,179, and 15,208 -> 4,187 characters. All eighteen panels pass `validate_storyboard_metadata_rows`; semantic fragments and CAMERA gaps are both empty on every board. Deterministic manifest `data/quality-manifests/grun_814d284c8c6f-current-code.json` passes; its visual gate remains `needs_review` because it deliberately reuses the prior paid images rather than claiming a new visual proof.
- Verification passes: 60 focused tests, 258 affected tests, all 823 backend tests, storyboard recipe genericity, compilation, duplication ownership, and `git diff --check`. The user's Character sheet, successful Environment, 17-node graph, reference order, provider/model/pricing settings, and unrelated Time Stop changes were not modified.
- PAID-2407 completed 17/17 as `grun_531ac2847b59`. Board 1: `job_9f8bb2ae10e2` / `asset_20aedc4047e0`; Board 2: `job_31c2f708da22` / `asset_25422c631328`; Board 3: `job_7ccd6963d697` / `asset_ab16097ba169`. All are 2048x1152. Balance 379.6 -> 349.6, exactly 30 credits / $0.15; three jobs completed with no retry or duplicate submission.
- Visual review passes the identical footer-free 3x2/six-row layout family, differentiated board identity, cinematic physical-set treatment, recurring pilot/ship/hangar continuity, causal 18-frame progression, both adjacent-but-distinct handoffs, attributed dialogue, feline Bolts, and launch payoff. Board 1 Panel 6 now holds the closed amber panel and Board 2 Panel 1 advances the latch; Board 2 Panel 6 holds green and Board 3 Panel 1 advances toward the ramp.
- Historical PAID-2407 metadata acceptance failed. Its submitted prompts contained incomplete values including `The pilot follows the status-light sequence until.`, `The pilot's mechanical hand grips;`, `The pilot locks the retaining clips around;`, `Boots contact successive.`, and `The cracked floor, painted markings.` Most Board 2-3 CAMERA rows collapsed to only `Live-action cinema camera.` despite the shared angle/movement/lens contract. BUG-2408 now closes this deterministic guard gap for future submissions; the old paid images remain unchanged evidence.
- The retained Character reference remains unchanged as requested, but exposed waist persists despite opaque-coverall prompt instructions. Treat this as a known user-reference limitation; do not swap the sheet without explicit direction.
- Manifest `data/quality-manifests/grun_531ac2847b59.json` reports deterministic `pass`, visual `needs_review`, overall `not_accepted`; the visual scorecard records V001 and V005 failures. Browser closure shows 349.6 credits, approximately 0 credits / $0, all six Storyboard GPT/Save and Environment GPT/Save nodes Frozen, and the unrelated Time Stop tab's unsaved changes preserved.

- Phase 24 paid run `grun_466f601a6e3d` completed 17/17 nodes. Board 1: `job_b7219cfa11d9` / `asset_793d26013f2c`; Board 2: `job_e7bcd7598cbb` / `asset_a978b1d79bbc`; Board 3: `job_272ad17669c6` / `asset_b7750eef86da`. All are 2048x1152. Balance 409.6 -> 379.6, exactly 30 credits / $0.15; 698 total media jobs and zero active jobs.
- Visual review passes identical footer-free 3x2/six-row layout, differentiated titles/sequence values, cinematic physical-set treatment, recurring covered pilot, stable ship/hangar Environment, causal repair/boarding/launch flow, attributed dialogue, and unmistakably feline cyborg-cat Bolts. It fails strict V007 because Board 1 Panel 6 drifts back to a ramp/loading view rather than preserving Panel 5's closed amber service-panel state for Board 2 Panel 1.
- The source recipe contained the correct closed-panel handoff. Post-run BUG-2406 fixes the generic compactor: spatial `before` is no longer cut as a temporal connector, primary action before `while` is preserved, multi-clause fragments and incomplete predicate/preposition tails fail closed, and narrative-row budgets reserve the smallest complete clause. No campaign term was added to shared code.
- Final zero-credit Browser run `grun_1b3b3ad70980` completed 17/17 at $0 with media jobs unchanged at 698 and no active job. Its three freshly regenerated raw outputs shape to 4,129 / 4,198 / 4,194-character provider prompts, six valid panels each, and zero semantic fragments. Board 1 Panel 6 now explicitly retains the amber-lit service panel and hand beside the latch. Affected suite: 245 passed; genericity and `git diff --check` pass.
- Deterministic manifest `data/quality-manifests/grun_466f601a6e3d.json` passes; strict visual gate is `needs_review` and overall is `not_accepted` solely on V007. Browser closure shows 379.6 credits, approximately 0 credits / $0, Sadis Adventures clean, and all six Storyboard GPT/Save plus Environment GPT/Save nodes Frozen. Media Studio remains running in detached screen `85408.media-studio`.

- PAID-2301 ran once as `grun_4adbeed15f4d`. Board 1 completed as `job_d3bb5b05a5f3` / `asset_c97abb7d31e7`; Board 2 completed as `job_f4545c9c3535` / `asset_de8b86098d98`; Board 3 `job_8f06af34e56e` failed with `Internal Error, Please try again later.` before any Board 3 asset/save. Balance moved 429.6 -> 409.6, exactly 20 credits / $0.10 for the two successful outputs; the failed provider task did not consume another 10 credits.
- Full-resolution review passes the shared footer-free 3x2 design, differentiated Board 1/2 titles and production strips, photoreal cinematic treatment, stable pilot/ship/hangar identity, Board 1's closed amber service-panel ending, Board 2's distinct latch-opening continuation, and causal repair progression. The retained Character reference still pulls exposed-waist styling.
- Final acceptance fails because there is no Board 3 output and Board 2 contains semantically incomplete metadata even though `validate_storyboard_metadata_rows` reports PASS with zero heuristic fragments. The deterministic trilogy auditor correctly refuses the partial run because no completed Board 3 artifact exists.
- Browser closure shows 409.6 credits, approximately 0 credits / $0, Sadis Adventures clean, Run available, and all six Storyboard GPT/Save nodes restored to Frozen; Environment GPT/Save remain Frozen/Cached. Database state is 695 media jobs, 741 graph runs, and zero active jobs.
- BUG-2205 is complete with no paid/provider run. The shared provider boundary rejects dangling ACTION/MOTION/NOTES fragments, repairs them from same-panel user/recipe-owned values before budget distribution, preserves concise complete clauses and exact dialogue, and caps budget redistribution at source length to prevent saturation loops.
- Exact frozen replay passes for Boards 1-3: 4,198 -> 4,119 (`metadata_normalized`), 14,135 -> 4,181 (`storyboard_compact`), and 4,156 -> 4,120 (`metadata_normalized`); every board validates six complete metadata sets with zero semantic fragments, and all three attributed dialogue values remain present exactly once.
- The saved `Sadis Adventures` workflow contains the positive Board 1 closed-state lock and Board 2 opening assignment with 17 nodes / 28 edges. In-app Browser inspection shows no Sadis unsaved badge, 429.6 credits, approximately 0 credits / $0, all eight Environment/Storyboard GPT+Save nodes Frozen, no console warnings/errors, and Run available but untouched. Current database state is 692 media jobs, 740 graph runs, and zero active media jobs.
- Focused post-fix verification passes 28/28; trilogy/metadata/prompt regressions pass 33/33; full backend passes 789/789; storyboard genericity and `git diff --check` pass. Media Studio remains healthy in detached `media-studio` with web HTTP 307-to-app and API `/health` HTTP 200.

- Phase 22 paid evidence consists of Board 1 `job_89bb63c6bbc7` / `asset_49f3103fd168`, followed by repaired continuation run `grun_cdaa9401bbbe` with Board 2 `job_225ce43e1dff` / `asset_d82c55094f72` and Board 3 `job_fc801cfa9df1` / `asset_139ca5940aa0`. All are 2048x1152. Total accounting is exactly 30 credits / $0.15; balance 459.6 -> 429.6.
- The first run `grun_83c378c36f03` spent only Board 1's 10 credits, then correctly stopped before Board 2 submission when Panel 04 NOTES was empty. A generic row-budget fix now reserves a readable floor for ACTION, MOTION, and NOTES before distributing larger preferred budgets; exact replay of the failed Board 2 prompt passes the preflight and preserves exact dialogue without campaign text in shared code.
- Visual review passes one matching footer-free 3x2 layout, exact differentiated board headers/production strips, feature-film still treatment, recurring pilot/ship/hangar continuity, causal repair/boarding/launch progression, adjacent-but-distinct handoffs, and unmistakably feline Bolts. It fails final content acceptance on semantic fragments such as `Panel opening is.`, `Only the clean.`, `The pilot begins.`, and `Final payoff —.`, plus the early-open Board 1 compartment.
- Focused metadata/preflight tests pass 18/18; exact failed-prompt replay passes; full backend passes 780/780; the storyboard genericity guard and `git diff --check` pass. The live server remains healthy in detached session `media-studio`, queue 0/0. Browser closure shows 429.6 credits, eight Frozen paid model/save nodes, approximately 0 credits / $0, Run available, and no active run.

- Phase 21 adds a generic fail-closed storyboard metadata preflight after final shaping and before KIE request construction/submission. Every panel must contain exactly one non-empty SHOT/CAMERA/ACTION/MOTION/NOTES value; only silent DIALOG may be blank; placeholders fail with an exact panel/row error before any provider job exists.
- Storyboard v2 is `2.19`, Continuation is `1.14`, and migration 47 refreshes existing installations. Missing NOTES reuse only same-panel user/recipe-owned MOTION/ACTION/SHOT state; no campaign story data was added to shared code.
- Focused tests pass 32/32 plus the 12-test preflight module. Full release verification passes 777 backend and 758 web tests, lint, typecheck, production build, Studio browser smoke, clean schema 47 bootstrap, genericity, file-size/style/hygiene, and diff formatting.
- Live Media Studio is healthy on ports 3000/8000 with schema 47, both new recipe versions, runner healthy, queue 0/0, and media jobs unchanged at 689. After the user refreshed the page, the in-app Browser verified Sadis Adventures at 459.6 credits and approximately 0 credits / $0, all eight Environment/Storyboard GPT and Save nodes Frozen, both prompt-only Environment nodes Muted, and zero console warnings/errors. No Run click or provider action occurred.
- Phase 20 now uses one shared six-row contract in exact `SHOT / CAMERA / ACTION / MOTION / DIALOG / NOTES` order. CAMERA owns angle/movement/lens plus shot size/placement; NOTES remains a mandatory label with an optional blank value. Storyboard v2 is `2.18`, Continuation is `1.13`, and schema migration 46 updates existing installations.
- Prompt shaping no longer moves clipped ACTION tails into NOTES, preserves complete row-local clauses, merges legacy FRAMING into CAMERA, reserves exact dialogue, removes orphan pronoun fragments, and reuses adjacent user/recipe-owned ACTION/MOTION content when one required row is blank. Shared code remains campaign-agnostic.
- Focused verification passes 415 relevant tests plus 21 post-proof regression/migration tests. Corrected zero-credit browser proof `grun_41a60964d9b6` completed 17/17 at $0 with media jobs unchanged at 686. Current-code replay of all three live raw prompts produces six instances of every label, no FRAMING, and no dangling clauses.
- PAID-2005 completed as recovered run `grun_97a000ce7558`: Board 1 `job_f79af5a22caa` / `asset_d539ca196281`; Board 2 `job_dd3fbdb6778c` / `asset_b593a92980ab`; Board 3 `job_03e334ff13e6` / `asset_1fba3d65bc29`. All are 2048x1152. Balance 489.6 -> 459.6: exactly 30 credits / $0.15, with no duplicate provider submission.
- Full-resolution review passes the common footer-free 3x2/six-row layout, exact differentiated board identity, photoreal feature-film still treatment, shared ship/hangar Environment, covered recurring pilot, causal walk-up/inspection/repair/boarding/lift flow, both adjacent-but-distinct board handoffs, and an unmistakably feline cyborg cat. Minor renderer residuals: Board 1 Panel 5 leaves the visible ACTION value blank despite the story image/other rows carrying the beat, and dialogue gains a period after its closing quote. The post-run generic ACTION/MOTION fallback fixes the former at the current provider boundary without buying another proof.
- BUG-2006 is complete: poll-failed Graph runs are recoverable, completed submitted jobs are reused, and downstream nodes skipped only because of `upstream_failed` resume without resubmitting paid work. Muted skips remain terminal. Focused recovery and trilogy-quality target passes 10 tests.
- The deterministic and visual manifest `data/quality-manifests/grun_97a000ce7558.json` is an overall pass. All six Board GPT/Save nodes are frozen to this exact run, the Environment remains frozen, the queue is empty, and the persisted workflow estimates 0 credits / $0.

- Phase 20 AUDIT-2001 compared the raw recipe output, stored provider prompt, current-code replay, displayed Graph Studio caches, and original Board 2/3 images. The visible Board 3 robot is `asset_f0333fcf7843` from PAID-1906, not a post-BUG-1907 result; PAID-1909 stopped at Board 2 and never generated a new Board 3.
- The old PAID-1906 Board 3 prompt used generic 4,194-character slicing and ended before the panel plan. Current-code replay of that exact 14,533-character raw output selects `gpt_image_2_storyboard_compact`, emits all six panels, retains feline/cat construction, and preserves the exact Bolts line.
- The displayed Board 2 is the older `job_fb692f0603fa` artifact and visibly has blank CAMERA/MOTION values in Panels 3-4. PAID-1909's newer Board 2 provider prompt contains non-empty CAMERA and FRAMING in all six panels, but `job_098d48bfaf98` failed at the provider, so the saved image remained the old cache.
- Dialogue is not intentionally moved between metadata rows. Exact DIALOG values are reserved first and dialogue-heavy cells receive extra budget. The residual defect is budget competition: ACTION is clipped to 120 characters, its tail is synthesized into NOTES, and the per-cell minimum order can starve or over-compress CAMERA/FRAMING/MOTION.
- CONTRACT-2002 is the next implementation task: replace the separate CAMERA and FRAMING rows with one CAMERA row ordered as camera angle/movement/lens followed by shot size/subject placement; keep DIALOG exact and row-local; keep NOTES blank unless the recipe/user supplied a useful note. This visible recipe/layout contract change must be implemented generically across the shared storyboard recipes and compact shaper.
- Focused read-only verification passes: `test_storyboard_prompt_regressions.py` plus `test_storyboard_trilogy_quality.py` = 7 passed. In-app Browser inspection confirms the 17-node graph, 489.6-credit balance, Board 1 frozen to PAID-1909, Boards 2-3 still displaying PAID-1906-era assets, and the Environment frozen/cached. No graph run, provider request, workflow edit, or credit spend occurred.

- PAID-1909 attempted as `grun_4983b8fb3503`. Board 1 job `job_2df9c121411d` completed as 2048x1152 `asset_2e64119ce721`; Board 2 job `job_098d48bfaf98` failed after 5.1 seconds with provider `Internal Error, Please try again later.`; Board 3 dependency-skipped. Balance moved 499.6 -> 489.6: 10 credits / $0.05 for Board 1 only.
- Board 1 visual review passes the fixed no-footer 3x2 sheet, correct production strip, all seven exact stacked labels in all six cells, cinematic live-action physical-set quality, prominent locked ship/hangar Environment, distant pilot/droid opening, causal inspection progression, closed-panel ending, and covered-waist wardrobe.
- PAID-1909 does not pass exact metadata-value acceptance. The provider-bound compact prompt redistributed clipped tails into `NOTES`, producing fragments such as `toward it.`, `secure`, and `is visibly climbing.`; Panel 6 also splits the intended action/motion across incomplete clauses. The raw Storyboard v2 recipe output contains complete clauses, proving this is deterministic compact-shaper loss rather than recipe/user-input loss.
- The Board 2 failure is transient provider infrastructure, not moderation or policy: submission validation passed, `gpt_image_2_storyboard_compact` produced 4,177 characters, the job failed on the first provider poll, and the account did not lose another 10 credits.
- The new Board 1 GPT/Save artifacts are frozen to `grun_4983b8fb3503`. Boards 2-3 remain frozen to PAID-1906 `grun_eb3d5d6c9e09`; the Environment remains frozen to its prior exact artifact. The saved browser workflow estimates approximately 0 credits / $0.
- BUG-1910 is the next no-paid task: preserve complete metadata clauses in their originating rows and leave absent `NOTES` blank instead of moving sentence tails into them. After focused/full verification, any new complete paid trilogy requires separate authorization.

- PAID-1906 completed as `grun_eb3d5d6c9e09`: three 2048x1152 boards, assets `asset_be6d19cd17a5`, `asset_4c5d1589625a`, and `asset_f0333fcf7843`; 30 credits / $0.15; immediate balance 549.6 -> 519.6; no retries.
- Visual review proves a matching 3x2 sheet family, seven stacked metadata rows per cell, much stronger live-action/physical-set rendering, shared ship/hangar continuity, and a causal inspection-to-repair-to-launch sequence. It is not accepted: Board 2 metadata values are over-compressed, Board 3 loses intended feline/dialogue/wardrobe details and shows title overload, and the retained Character reference still contributes exposed-waist and slight CGI styling.
- BUG-1907 identified the Board 3 cause after the paid run: uppercase `PANEL NN IMAGE:` sections were not classified as storyboard content, and `sheet` was incorrectly treated as a private name. Storyboard recognition is now case-insensitive and structural; metadata compaction uses complete semantic clauses and dialogue-aware per-panel budgets; all story content remains user/recipe-owned rather than campaign hardcoding.
- Current-code replay of the exact paid Board 3 recipe output uses `gpt_image_2_storyboard_compact` at 4,200 characters, preserves six panels with all seven labels, articulated feline forelegs, covered wardrobe, and the exact Bolts line. Storyboard v2 is `2.16`; Continuation is `1.11`.
- Full release verification passes 759 backend tests, 758 web tests, lint, typecheck, production build, Studio browser smoke, clean schema 44 bootstrap, repo hygiene, genericity, file-size, style-drift, and diff-formatting gates.
- The six storyboard GPT/Save nodes are frozen to PAID-1906's exact artifacts. Final zero-cost in-app Browser proof `grun_5de1d1649627` completed 17/17 with all six cached from `grun_eb3d5d6c9e09`, actual graph cost $0, and zero new local media jobs.
- The browser later displayed 499.6 credits, 20 below the immediate post-paid balance. The proof run cannot account for that delta: its metrics report $0 and the local media-job count did not change. Treat the 20-credit change as external/unattributed pending provider-account reconciliation.
- Phase 19 zero-credit preflight `grun_70c7349b9dc9` completed 17/17 at $0 with zero media jobs. All three provider-bound prompts require seven separate full-width horizontal metadata rows and prohibit side-by-side columns, tables, vertical lanes/dividers, merged labels, and multi-label rows.
- Paid proof `grun_76a44838e61e` completed Boards 1-2 as `asset_9d0be5fd5fd4` and `asset_666a05115bcf`. Board 3 job `job_6eeab65cfe5c` was rejected by the provider content filter and consumed zero credits. Balance moved 569.6 -> 549.6: 20 credits / $0.10 for the two successful boards, with no retry.
- Visual review proves stacked metadata rows and shared chrome on both successful sheets. Board 2 renders all six cells and all seven rows. Board 1 is not acceptable because Panel 6 is blank, Panel 3 clips `SHOT` to `SHO`, and some row values are truncated. The saved accepted trilogy was not replaced.
- The browser was restored to eight Frozen nodes and approximately 0 credits / $0 after the partial run. Character and Environment references remain unchanged.
- Startup recovery now skips an incompatible historical interrupted-run snapshot instead of preventing Media Studio startup; the full Graph Studio backend suite passes 176/176.

- PAID-1810 completed as `grun_e83a12b31a07`: assets `asset_9a1446151ab0`, `asset_d8d58a004fa5`, and `asset_9a63be43b6f6`; all are 2048x1152; three KIE jobs completed; balance 599.62 -> 569.62 (30 credits / $0.15); no Environment generation or Seedance job.
- The post-fix provider prompts preserve exact user-owned production metadata, handoff, dialogue, wardrobe, and subject-design directives. Visual review passes feline cyborg-cat Bolts, exact Board 1/2/3 titles and production-strip values, strong shared hangar/ship continuity, a consistent no-footer six-panel sheet family, attributed droid/pilot dialogue, and causal Board 2-to-3 movement.
- PAID-1810 is not a full visual acceptance: the retained Character reference still drives exposed-waist styling; Board 1 Panel 6 opens the service bay before the requested closed-panel handoff; Board 2 renders `FRAMRA` and `ACTING` label typos; and Board 3 ends at launch preparation rather than visible lift-off/departure.
- Two zero-charge pre-provider attempts, `grun_63e4aff8eb33` and `grun_f5c84c3c4c80`, exposed a stale-provider-catalog render loop and transient Codex App Server reconnect misclassification. Both defects are fixed with focused regressions; the full image-capable App Server probe passed before PAID-1810.
- Six storyboard GPT/Save nodes are frozen to PAID-1810's exact artifacts. Clean-browser zero-credit proof `grun_5f1f3974594e` completed 17/17 nodes with all eight paid/save nodes Frozen/Cached, media jobs unchanged at 678, balance 569.62, and zero errors/warnings.
- Final post-PAID-1810 release verification passes 755 backend tests, 758 web tests, lint, typecheck, production build, Studio browser smoke, clean schema 44 bootstrap, genericity/file-size/style/hygiene guards, and diff formatting.
- PAID-1807 completed as `grun_3c59a437ce9a`: three 2048x1152 storyboards, assets `asset_bf84092e3022`, `asset_b8b3fb7f749f`, and `asset_ffcc8edf0d55`; 30 credits / $0.15; balance 629.6 -> 599.6; no Environment generation or Seedance job.
- Visual review passes identical no-footer sheet geometry, differentiated Board 1/2/3 titles and sequence numbers, readable story progression, Board 1-to-2 repair handoff, and complete attributed dialogue. It fails final acceptance because the exposed waist remains, Bolts is humanoid, Board 2 Panel 6 -> Board 3 Panel 1 jumps too far toward the ramp, and the production strip invents PROJECT/DATE/ARTIST values.
- The run exposed BUG-1808: submitted provider prompts preserved board titles but dropped user-owned production metadata, wardrobe cues, and subject-design cues; Board 3 contained neither `feline` nor the non-humanoid constraint. The sanitizer now appends all six user-owned directives before compaction and applies equally to the base and Continuation recipes.
- Full release verification after the preservation fix passes 752 backend tests, 757 web tests, lint, typecheck, production build, Studio smoke, clean schema 44 bootstrap, genericity guard, and diff formatting. The final Continuation dispatch addition passes 16 focused sanitizer/shaper tests and the full 175-test Graph Studio suite.
- Frozen persistence is repaired to the paid run's exact artifacts. Zero-cost proof `grun_3288fa193219` completed 17/17 nodes with no new media jobs; a fresh in-app Browser tab shows all three PAID-1807 paths as Frozen/Cached at 2048x1152, eight frozen nodes, 599.6 credits, approximately 0 credits / $0, and no browser errors.
- Post-fix browser run `grun_643471f2d8b9` completed at $0. All three live prompt previews expose labeled production metadata, dialogue, wardrobe, and subject-design cues; Boards 2-3 expose handoff directives; Board 3 contains feline and non-humanoid constraints. Three pre-run hot-reload update-depth errors did not recur after a clean reload.
- The post-run code fix received its paid visual proof in PAID-1810. No paid retry is authorized.

- Phase 18 is complete. Shared storyboard recipes and compilers are now story-agnostic, Storyboard v2 is `2.14`, and Storyboard Continuation is `1.9`. User-owned `board_title`, `production_metadata`, `handoff_advance`, `dialogue_cues`, `wardrobe_cues`, and `subject_design_cues` drive campaign-specific content.
- `scripts/check_storyboard_recipe_genericity.py` is a mandatory quality gate. It rejects campaign names, subjects, locations, props, and story actions from reusable recipe/compiler owners. Story-specific quality checks now enter through a `TrilogyQualityContract` supplied by user/workflow data rather than module constants.
- The saved `Sadis Adventures` workflow contains three distinct board titles/sequence metadata sets, two adjacent-but-distinct handoff actions, explicit covered-workwear cues, speaker/voice cues, and an unmistakably feline/non-humanoid Bolts design cue. Character and Environment references are unchanged; all eight paid/save nodes remain frozen; estimate is approximately 0 credits / $0.
- Full release verification passes: 751 backend tests, 757 web tests, web lint, typecheck, production build, Studio browser smoke, clean migration bootstrap through schema 44, and diff formatting. In-app Browser verification passed after closing and reopening the saved workflow so the canvas rehydrated the latest persisted fields.
- These changes now reach the provider boundary deterministically, but a paid retry is still required to prove the post-run fix visually. The retained Character sheet continues to conflict with the covered-waist instruction, and a dedicated feline reference would remain stronger evidence than text alone.

- Phase 17 review remediation is complete. CR-001/002/003/005/006 are resolved; migration 43 is applied to the live database; focused backend is 367/367; full API is 750/750; the complete 132-file web suite, lint, typecheck, production build, Studio smoke, clean migration, HTTP health, and diff formatting pass.
- In-app Browser reload proves the 17-node `Sadis Adventures` canvas, 30-edge saved sequence, 629.6-credit balance, zero-cost frozen estimate, shared Environment references, and user-owned dialogue/wardrobe/subject fields. A discovered manual-resize React update loop was made idempotent and produced no new browser errors after reload.
- Review report: `docs/reviews/20260712-042852-media-studio-uber-review.md`. Reachable legacy skill-id, old-attachment, and helper-facade compatibility paths were retained intentionally; TASK-011 tracks test/coordinator extraction before lowering reviewed file-size caps.

- Phase 16 paid run `grun_d7da4a50b807` completed three 2048x1152 boards for 30 credits / $0.15; balance 659.6 -> 629.6. It visibly proves identical no-footer sheet geometry, the shared hangar/ship, causal story progression, and explicit DROID/PILOT voice attribution.
- Phase 16 is not a final visual signoff: the retained Character reference still drove exposed-waist styling, Bolts rendered as a humanoid robot rather than a feline cyborg cat, and Board 3 Panel 1 remained too close to Board 2 Panel 6. The run is preserved/frozen without retry.
- Shared prompt shaping no longer injects campaign nouns or story beats. Storyboard v2 `2.14` and Continuation `1.9` expose user-owned board identity, dialogue, handoff, wardrobe, and subject-design cues; the selected workflow is prepared with those fields for a future paid proof.

- Phase 15 proved the requested visual rule. Paid run `grun_96397a59122a` created `asset_919583f09e54`, `asset_c5240bd45474`, and `asset_9f90738c27b9` for 30 credits / $0.15; balance 689.6 -> 659.6. Both cross-board handoffs pass as near-identical adjacent takes.
- The Phase 15 trilogy was not selected overall because page numbering, sheet chrome parity, covered wardrobe, Bolts design, and lift separation regressed. All outputs are preserved, while the saved workflow freezes Phase 12 best `grun_e39207d96114`.
- The compact provider contract now includes deterministic gate D011 and an explicit `@image3` Panel-06-to-Panel-01 camera/framing/state match lock. Full relevant backend target: 177 passed.

- Phase 13 paid proof `grun_9e02c78b4ce0` completed all three 2048x1152 boards for 30 credits / $0.15; balance 719.6 -> 689.6. Its deterministic gate passes, but its strict visual gate fails at the early-open Board 1 service bay, Board 3 `DIALOS` label, and incomplete Panel 5 lift/Bolts proof. No retry was attempted.
- Phase 13 closure restored and froze all six Board GPT/Save nodes to Phase 12 best `grun_e39207d96114`. Character `asset_901adc9b21d1` and the frozen Environment were unchanged; no Seedance job exists.
- REVIEW-1305 report: `docs/reviews/20260711-173000-recipes-presets-media-assistant.md`. Confirmed 2 High / 2 Medium findings and documented eight remediation/verification tasks. Focused backend is 343 passed / 9 failed; focused frontend is 94/94 passed.

- Phase 12 best trilogy: `grun_e39207d96114`, assets `asset_362ab770ad2b`, `asset_e2f7c447794f`, and `asset_cf80b084f4ff`. It is frozen in the saved 17-node/30-edge workflow and the in-app Browser reports approximately 0 credits / $0.
- Phase 12 accounting: six full trilogy runs, 18 new boards, 180 credits / $0.90; browser balance 899.6 -> 719.6. Two pre-provider interruptions (`grun_aa382ee15e5b`, `grun_cc64b46ca4b7`) created no media job and cost $0.
- Final quality verdict: attempt 5 is the only same-run set that combines the distant Board 1 pilot/droid/Environment opening, matching PAGE 1/3-3/3 full-sheet design, covered mechanic workwear, causal repair, first Bolts reveal in Board 3 Panel 3, complete dialogue, and coherent departure. Residual: Board 3 Panel 5 shows an exterior ship through the cockpit canopy, making lift proof visually ambiguous before the correct airborne Panel 6.

- Completed: all Phase 0-7 deterministic work, BUG-712 reference replacement, explicit Environment carry-forward, four reference-safety no-paid runs, PAID-713, and BUG-714 post-failure hardening.
- Terminal failure: PAID-713 `grun_b005f9a1dbf8` failed at Board 1 moderation; job `job_02c012a45027` used the intended two ordered references and Boards 2/3 dependency-skipped.
- Terminal visual result: Board 1 generated, but clipped metadata values, exposed-midriff wardrobe drift, and a prematurely opened final service panel prevent a sequence-quality pass.
- Complete: the accepted sequential Board 1 -> Board 2 -> Board 3 chain is frozen and browser-verified at zero estimated cost.
- Phase 11 attempt 1: `grun_3abd7136960e` created `asset_cf512c0663cc`; strong layout/environment but rejected for wardrobe, final handoff, and visible internal-state phrasing. Credits are 939.62; jobs/runs are 641/695.
- Residual visual note: Board 1 omits the compact top production strip and bordered footer area used consistently by Boards 2-3; the six-panel geometry and all per-frame metadata rows match across all three.
- BUG-715 review: `docs/reviews/20260710_223334/` has zero open findings after the resize-hit-target and authoritative-clean-tab fixes.

## Current Evidence Snapshot

- Repo: `/Users/evilone/Documents/Development/Video-Image-APIs/media-studio`
- Active workflow: `graphwf_4fd06f50c493` / `Sadis Adventures`, 17 nodes and 30 edges after repair.
- Final accepted chain: Board 1 `asset_60c6f076511e` from `grun_e1370bed4813`; Board 2 `asset_a50774289804` from `grun_e8206374dbeb`; Board 3 `asset_ca6180f66b9a` from `grun_62ae7afc8796`.
- Phase 11 accounting: five paid runs, 50 credits / $0.25 total; browser balance 949.6 -> 899.6; final queue 0/0.
- Final no-paid proof: `grun_4dab614d88dd`, completed 9 local/preview nodes plus 8 skipped paid/save nodes with media jobs unchanged at 636.
- Repaired graph: Character and Environment use typed recipe ports; Board 2/3 receive the immediate prior board through `additional_refs`; GPT reference order is Character 1, Environment 2, previous board 3.
- Persisted repair isolation: all 266 other workflow hashes and run/asset/job counts remained unchanged.
- Prompt shaper: labeled non-freeze story briefs ignore conditional freeze-template prose; positive freeze briefs retain `NORMAL -> FREEZE TRIGGER -> FROZEN INTERVENTION -> UNFREEZE TRIGGER -> RESUMED` order.
- Final submitted-prompt audit: Environment 4,185 chars, Board 1 3,021, Board 2 3,450, Board 3 3,445; storyboards retain ordered refs, required story terms, and no time-freeze/private/provider contamination.
- Preview lifecycle: Environment and Boards 1-3 retained `asset_d4ab90a294ae`, `asset_199a95569540`, `asset_a523dece2157`, and `asset_b3333ad9da1c` through muted model/save nodes.
- Runtime sanity: the local development stack was restarted on web `:3000` / API `:8000`; API and runner are healthy, queues are empty, Graph Studio returns HTTP 200, and Chrome loads the 30-edge canvas.
- Phase 5 outputs: new Environment `asset_c32387090bc8` and Board 1 `asset_8e7579450185`; Board 2 job `job_952f57817b16` failed provider moderation; Board 3 never submitted; 20 credits consumed.
- BUG-501 fix: `CELL NN` blocks normalize into ordered panel capsules; explicit action/dialogue/notes and final handoffs survive bounded shaping; storyboard classification wins over incidental environment wording; covered-wardrobe and neutral non-sexual framing are enforced at the submission boundary.
- Exact Phase 5 replay: Board 1 raw 8,718 -> shaped 3,488 chars and Board 2 raw 11,899 -> shaped 3,456 chars; both keep panels 01-06, required story beats, safety language, and no `dialogue: notes` parser bleed.
- Safety state: all four KIE image and four save nodes are remuted; saved workflow validates with zero errors/warnings and the browser reports approximately 0 credits / $0.
- Phase 7 preparation: backup `data/backups/media-studio-backup-20260710T103717Z.db`; target graph remains 17 nodes / 30 edges; all four save nodes target Sadi; non-target workflow hash remains `7cca868ae7e30838f25235d5fec7ab2eb7ab78985bea6226888baa0a4d864bed`; saved validation is valid with zero errors/warnings.
- First Phase 7 no-paid run: `grun_c50b03202bd0`, completed all recipe/preview nodes in order with media jobs unchanged at 636. Raw prompts were Environment 6,890, Board 1 8,786, Board 2 11,363, Board 3 12,832 chars. Shaped replay stayed under budget but failed SPEC-012 by abbreviating/dropping `CAMERA`, `FRAMING`, `ACTION`, `MOTION`, `DIALOG`, and `NOTES`; Board 1 also lost several explicit inspection terms.
- BUG-706 evidence: focused red -> green; 9/9 prompt-shaper tests; full Graph Studio API target `153 passed in 78.88s`; `py_compile` and `git diff --check` pass. Exact Phase 7 replay now gives all six panels all seven exact labels; shaped lengths are Environment 4,192, Board 1 3,697, Board 2 3,922, and Board 3 3,643 chars.
- Second Phase 7 no-paid run: `grun_6dc068e95249`, completed 9 local nodes / 8 muted skips with media jobs still 636. Environment raw 5,340 -> shaped 4,176 passes. Storyboard raws use `Panel NN image —` headings and currently fall back without capsules; Board 3 also copied Board 2's title despite otherwise correct boarding/cat/launch content.
- BUG-707/GRAPH-708 evidence: exact live heading fixture red -> green; `Panel NN image —` and explicit `titled “...”` are preserved; 10/10 shaper tests; full Graph Studio API target `154 passed in 76.18s`; `py_compile` and `git diff --check` pass. Board 3 title lock is saved with all eight paid/save nodes still muted.
- BUG-709 evidence: split reference paragraphs, `titled exactly`, fixed-grid spatial headings, neutral subject title suffixes, and late causal state terms are covered by 14 focused cases. Full Graph Studio target passes `158 passed in 76.16s`; final current-code replay is Environment 4,176, Board 1 3,711, Board 2 3,851, Board 3 3,886.
- PAID-704 preflight: backup `data/backups/media-studio-before-paid-704-20260710T115915Z.db` SHA-256 `2e3a1602249b94ae17fad6ae41a7ea22afe5d88ac683b96b56f97122c7caa610`; 17/30 graph; queue empty; 636 jobs; 969.6 credits; estimate 40 credits / $0.20; no Seedance.
- PAID-704 result: Environment job `job_9f8e6328df4d` completed and created `asset_123e29b65e70` at 2048x1152. Board 1 job `job_7edc89e85f74` failed moderation; only two jobs submitted; credits ended at 959.6, 10 lower.
- Environment review: strong pass for one fixed red/cream ship, orthographic consistency, hangar routes, service panel, open ramp, explicit ramp/corridor/cockpit connection, adjacent pilot/cat chairs, harnesses, materials, lighting, and takeoff path; minor small-text corruption/invented `Hangar Bay 7` remains.
- Reference-risk evidence: Board 1 submitted 3,405 safe-guarded characters with Character `@image1` and new Environment `@image2` in order. The actual character sheet visibly repeats a bare midriff/cropped suit plus private name/age, directly conflicting with the covered/private-text prompt contract. This is the strongest observed moderation contributor, not a proven provider cause.
- Phase 7 safe closure: all eight GPT Image/Save nodes were returned to safe execution states; queue empty; media jobs 638; all 266 non-target workflow hashes matched the pre-paid backup.
- Phase 8 BUG-712 reference result: Character temporarily used imported `ref_3c38d161cdc3` (`sadi-face_chest.jpg`) as a neutral identity-only portrait. Phase 9 intentionally returned to the user's browser-attached `asset_901adc9b21d1` sheet under explicit instruction not to replace it.
- Environment reuse: Environment GPT and Save are explicitly frozen to `grun_6ed69a0a6f86`, carrying `asset_123e29b65e70`; final no-paid run `grun_cb78cbc5ec4b` proved the exact asset reached downstream without a new provider job.
- Reference-safety no-paid gate: submitted-prompt replays are 3,391-3,756 chars; each board has six copies of SHOT/CAMERA/FRAMING/ACTION/MOTION/DIALOG/NOTES; ordered `@image1/@image2/@image3` roles and required story/dialogue survive; no private-name or redaction contamination remains.
- PAID-713 result: `grun_b005f9a1dbf8` / `job_02c012a45027` failed provider moderation at Board 1 after 131 seconds. Submitted prompt was 3,453 chars; reference 1 was `ref_3c38d161cdc3`, reference 2 was `asset_123e29b65e70`; credits remained 959.6; media jobs are now 639; queue is empty.
- BUG-714 evidence: exact paid raw `Top-left panel:` through `Bottom-right panel:` format now compacts to 3,528 chars with all six panels/seven rows, both ordered refs, all inspection beats, positive `fully enclosed crew workwear` and `task-focused professional framing`, and none of the observed negative moderation vocabulary.
- Current verification: focused prompt/sanitizer target 30 passed; full Graph Studio API target 163 passed in 81.09s; `py_compile` and `git diff --check` pass.
- UI defects encountered and reverted safely: Character `Replace` opens no scoped picker and global selection creates an unconnected 18th node; frozen cached nodes hydrate as Muted in Graph Studio. API fallback preserved 17/30 and versioned every target-only mutation.
- Final safety state: target graph validates with zero errors/warnings; board GPT/save nodes are muted; Environment GPT/save remain frozen to the successful asset; no Seedance node exists; all 266 non-target workflows retain hash `7cca868ae7e30838f25235d5fec7ab2eb7ab78985bea6226888baa0a4d864bed`.
- BUG-715 browser proof: selected Character replacement reused `ref_3c38d161cdc3`, stayed 17 nodes / 30 edges, and kept the target workflow hash `03709b73a640086035b69b65653226393fa4f36903af078323cdd5c50ff2d7ac`.
- BUG-715 run: `grun_5c92ebb07834` completed with 9 completed, 2 cached, and 6 skipped nodes at $0.00; Environment GPT/Save displayed Frozen/Cached with `cached_run_id=grun_6ed69a0a6f86`; media jobs remained 639 and queue remained empty.
- BUG-715 validation: 310 Graph Studio web tests, TypeScript typecheck, web lint (494 files), theme-drift guardrail, and `git diff --check` pass. Backup: `data/backups/media-studio-backup-20260710T151421Z.db`.
- PAID-716 preflight: backup `data/backups/media-studio-before-phase9-board1-20260710T154521Z.db` SHA-256 `f22e54c3042f06bce79633a1dbdf85eabda323df8f7931e007c4f08c3181ba32`, `quick_check=ok`; 17 nodes / 30 edges; queue empty; 959.6 credits; estimate 10 credits / $0.05; only Board 1 GPT/Save enabled.
- PAID-716 run: `grun_6c158a8dd274` completed in 349.75 seconds with 12 completed, 1 cached, and 4 skipped nodes. The only provider job, `job_64495d8b780d`, succeeded with a 3,398-character prompt and created 2048x1152 `asset_d9d0ccdff7fd`; credits ended at 949.6.
- VERIFY-717 browser review: PASS for six-panel 3x2 layout, stable hangar/ship geography, ordered metadata labels, blank dialogue, and causal inspection progression. FAIL for clipped metadata values, fully covered workwear, and the closed-panel final handoff. Boards 2/3 remained muted and submitted no job.
- Final saved state: Character `asset_901adc9b21d1` unchanged from the user's browser selection; Environment and Board 1 GPT/Save frozen; Boards 2/3 GPT/Save muted; graph estimate approximately 0 credits / $0; validation clean; no Seedance; all 266 non-target workflow hashes unchanged at `7cca868ae7e30838f25235d5fec7ab2eb7ab78985bea6226888baa0a4d864bed`.

## Next Safe Action

No further paid run is authorized. Review the preserved Phase 12 outputs or open a separately approved phase only if the residual lift-frame ambiguity must be addressed.

## Campaign State

- Campaign file: [CAMPAIGN.md](CAMPAIGN.md)
- Current approved phases: Phase 10 and Phase 11 complete; Phase 12 PLAN-1201 through VERIFY-1205 authorized.
- Campaign mode allowed: bounded prompt/workflow hardening, zero-cost proof, then at most six full paid trilogy runs with visual review.
- Persisted repair authorization: workflow `graphwf_4fd06f50c493` only, after code/tests pass and a restorable backup is created.
